// script.js
document.addEventListener('DOMContentLoaded', () => {
    const playButton = document.getElementById('play-quran');
    playButton.addEventListener('click', () => {
        // إضافة تفاعل لتشغيل تلاوة القرآن
        alert('سيتم تشغيل تلاوة القرآن!');
    });
});